const calculos = require("./calculos") // importar os objetos do module.exports

// console.log(calculos)

let x, y

x = 10
y = 5

console.log(calculos.soma(x, y))
console.log(calculos.sub(x, y))
console.log(calculos.texto)